import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";
import CTAFAQ from "@/components/layout/CTAFAQ";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title:
    "Shri Shyam Engineering | Panel Board and Agriculture Machinery Manufacturers",
  description:
    "Shri Shyam Engineering is a trusted manufacturer of high-quality electrical panel boards and reliable agriculture machinery. We combine innovation and durability to power industries and empower farmers across India.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        {children}
        <CTAFAQ />
        <Footer />
      </body>
    </html>
  );
}
